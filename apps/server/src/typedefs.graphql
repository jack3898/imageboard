scalar Date

interface File {
  id: ID!
  tags: [String!]!
  user: String!
}

type ImageFile implements File {
  id: ID!
  tags: [String!]!
  user: String!
  imageVariants: [Image!]!
}

type Image {
  id: ID!
  type: String!
  quality: String!
  path: String!
  width: Int!
  height: Int!
}

type User {
  id: ID!
  username: String!
  email: String!
  createdAt: Date!
  updatedAt: Date!
}

type Query {
  files: [File!]!
  loggedInUser: User
}
