query File($fileId: ID!) {
  file(id: $fileId) {
    id
    tags
    user
    title
    description
    createdAt
    resolveUser {
      username
    }
    ... on ImageFile {
      alt
      imageVariants {
        id
        path
        width
        height
        type
        quality
      }
    }
  }
}
