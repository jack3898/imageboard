query Posts($filter: String) {
  posts(filter: $filter) {
    id
    title
    isOwner
    file {
      variants {
        id
        path
        width
        height
        quality
      }
      type
    }
  }
}
