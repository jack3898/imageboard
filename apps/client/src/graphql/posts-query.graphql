query Posts($filter: String) {
  posts(filter: $filter) {
    id
    title
    file {
      variants {
        id
        path
        width
        height
        quality
      }
      type
    }
  }
}
